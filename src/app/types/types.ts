export interface Extension {
  logo: string;
  name: string;
  description: string;
  isActive: boolean;
}
export interface Extension {
  id: number;
  name: string;
  description: string;
  isActive: boolean;
  logo: string;
}
